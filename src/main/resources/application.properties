# 1. Dirección de MySQL y nombre de la base de datos (cámbiala si no se llama 'demo')
spring.datasource.url=jdbc:mysql://localhost:3306/demo?createDatabaseIfNotExist=true&serverTimezone=UTC

# 2. Credenciales de acceso
spring.datasource.username=root
spring.datasource.password=raquel2701

# 3. Driver de conexión
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 4. Configuración de Hibernate (Crea automáticamente las tablas en MySQL basadas en tus clases de Java)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

